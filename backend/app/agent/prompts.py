# backend/app/agent/prompts.py
INTENT_PROMPT = """你是运维知识库的意图分类器。结合对话历史，把用户最新问题分类为：
- ops_qa：运维技术问题（故障排查、产品使用、SOP 咨询）
- chitchat：闲聊、打招呼
- faq：与运维无关的通用问题
只输出分类标签本身。

对话历史：{history}
用户问题：{question}"""

DECOMPOSE_PROMPT = """判断下面的运维问题是否涉及多个云产品或多个独立知识点。
如果是，拆成 2~4 个可独立检索的子问题；如果不是，原样返回。
每行一个问题，不要编号以外的内容。

对话历史：{history}
用户问题：{question}"""

GENERATE_PROMPT = """你是天翼云运维专家。只根据下面的知识片段回答用户问题，遵守：
1. 每个关键结论后标注引用编号，如 [1]、[2]，编号对应片段序号；
2. 片段没有提及的内容不要编造；
3. 用运维人员能直接操作的步骤化语言回答。
4. 如果存在确认提示，必须将其原样放在回答开头。

知识片段：
{context}

对话历史：{history}
用户问题：{question}
确认提示：{confirm_notice}"""
